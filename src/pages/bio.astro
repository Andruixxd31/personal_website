---
import '../styles/global.css';
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle: string ='Sobre mi';

interface Identity {
  firstName: string;
  country: string;
  occupation: string;
  hobbies: string[];
}

const identity: Identity = {
  firstName: "Andrés",
  country: "Mexico",
  occupation: "ingeniero en sistemas computacionales",
  hobbies: ["correr", "box", "leer", "senderismo"],
};

const skills: string[] = ["HTML", "CSS", "JavaScript/Typescript", "React", "Astro", "Go", "Python", "linux", "sql", "mongo"];
const skillColor: string = "navy";
---

  <style define:vars={{skillColor}}>
    .skill {
      color: var(--skillColor);
      font-weight: bold;
    }
  </style>
  <BaseLayout pageTitle={pageTitle}>
    <p>Soy {identity.occupation}</p>
    <p>Me gusta mucho leer, escuchar musica, deportes de cardio y conocer personas y la manera en que perciben al mundo</p>
    <p>Trato de seguir aprendiendo y creciendo un poquito más cada día</p>

    <p>Mis conocimientos</p>
    <ul>
      {skills.map((skill) => 
        <li class="skill">{skill}</li>
      )}
    </ul>
  </BaseLayout>
